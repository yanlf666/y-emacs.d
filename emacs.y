

;;emacs 各文件路径
(add-to-list 'load-path (expand-file-name "~/y-emacs.d/emacs-config/"))
    (require 'init-base)
    (require 'elpa)
    (require 'plugins)
    (require 'org-mode)
    (require 'themes)




